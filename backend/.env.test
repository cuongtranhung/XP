# Test Environment Configuration
NODE_ENV=test
PORT=5001
FRONTEND_URL=http://localhost:3001

# Database (using test database)
DATABASE_URL=postgresql://postgres:@abcd1234@192.168.5.3:8080/fullstack_auth_test

# JWT
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=24h

# Security
BCRYPT_ROUNDS=4
RESET_TOKEN_EXPIRY=3600000
EMAIL_VERIFY_TOKEN_EXPIRY=86400000

# Email Configuration (mocked for testing)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=test@test.com
SMTP_PASS=testpass
FROM_EMAIL=noreply@test.com
FROM_NAME=Test App

# Rate Limiting (relaxed for testing)
LOGIN_RATE_LIMIT=100
REGISTER_RATE_LIMIT=100
FORGOT_PASSWORD_RATE_LIMIT=100
RESET_PASSWORD_RATE_LIMIT=100
GENERAL_RATE_LIMIT=1000

# Development
LOG_LEVEL=error